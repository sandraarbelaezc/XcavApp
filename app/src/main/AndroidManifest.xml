<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sandra.xcavapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity">
        </activity>
        <activity android:name=".RegistroActivity" />
        <activity
            android:name=".AdminR1Activity"
            android:label="@string/title_activity_admin_r1"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".AdminR2Activity"
            android:label="@string/title_activity_admin_r2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OperaR1Activity"
            android:label="@string/title_activity_opera_r1"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".OperaR2Activity"
            android:label="@string/title_activity_opera_r2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdmOpeR1Activity"
            android:label="@string/title_activity_adm_ope_r1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdmOpeR2Activity"
            android:label="@string/title_activity_adm_ope_r2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DrawerActivity"
            android:label="@string/title_activity_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".AddViajeActivity" />
        <activity android:name=".AddVoquetaActivity" />
        <activity android:name=".RetroActivity"></activity>
    </application>

</manifest>